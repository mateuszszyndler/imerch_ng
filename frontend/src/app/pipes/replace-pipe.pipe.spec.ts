import { ReplacePipePipe } from './replace-pipe.pipe';

describe('ReplacePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplacePipePipe();
    expect(pipe).toBeTruthy();
  });
});
